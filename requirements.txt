fastapi>=0.110.0
uvicorn[standard]>=0.29.0
Pillow>=10.0.0
pydantic>=2.0.0
anthropic>=0.40.0
python-multipart>=0.0.9
google-genai>=0.8.0
httpx>=0.27.0
reportlab
# stripe is only needed when BILLING_ENABLED=true; the app boots without it.
stripe>=9.0
